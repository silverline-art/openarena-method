EXMO GAIT ANALYSIS PIPELINE - ARCHITECTURE VISUALIZATION
=========================================================

1. MODULE DEPENDENCY GRAPH
==========================

     ┌─────────────────────────────────────────────────────────────┐
     │                    USER INTERFACE                            │
     │  ┌──────────┐              ┌──────────────────────────┐     │
     │  │  cli.py  │              │   batch_process.py       │     │
     │  │  (316L)  │ ─────────────│   (524L)                 │     │
     │  │          │  imports     │   - ProcessPoolExecutor  │     │
     │  └─────┬────┘              │   - Progress tracking    │     │
     │        │                   └──────────────────────────┘     │
     └────────┼──────────────────────────────────────────────────┬─┘
              │                                                  │
              │ run_pipeline()                                   │
              ▼                                                  ▼
     ┌────────────────────────────────────────────────────────────┐
     │               CORE PROCESSING MODULES                       │
     ├────────────────────────────────────────────────────────────┤
     │                                                             │
     │  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐  │
     │  │ data_loader  │──▶│ preprocessor │──▶│phase_detector│  │
     │  │   (220L)     │   │   (278L)     │   │   (294L)     │  │
     │  └──────────────┘   └──────┬───────┘   └──────────────┘  │
     │                             │                              │
     │  ┌──────────────┐          │           ┌──────────────┐  │
     │  │step_detector │◀─────────┘           │ metrics_comp │  │
     │  │   (318L)     │                      │   (694L)     │  │
     │  └──────────────┘                      └──────┬───────┘  │
     │                                                │           │
     └────────────────────────────────────────────────┼──────────┘
                                                      │
                                                      ▼
     ┌────────────────────────────────────────────────────────────┐
     │           STATISTICS & EXPORT MODULES                       │
     ├────────────────────────────────────────────────────────────┤
     │  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐  │
     │  │ aggregator   │──▶│xlsx_exporter │   │  visualizer  │  │
     │  │   (276L)     │   │   (246L)     │   │ (453+596L)   │  │
     │  └──────────────┘   └──────────────┘   └──────────────┘  │
     └────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
     ┌────────────────────────────────────────────────────────────┐
     │                  UTILITY SERVICES                           │
     ├────────────────────────────────────────────────────────────┤
     │  signal_processing (267L)  │  geometry (335L)              │
     │  validation (198L)          │  style (433L)                │
     └────────────────────────────────────────────────────────────┘


2. DATA FLOW PIPELINE
=====================

INPUT                PROCESSING              ANALYSIS             OUTPUT
─────                ──────────              ────────             ──────

3 × CSV         ┌──────────────┐      ┌──────────────┐     ┌──────────┐
 TOP    ───────▶│ DataLoader   │─────▶│ Preprocessor │────▶│ Excel    │
 SIDE   ───────▶│ - Parse CSV  │      │ - Filter     │     │ - 5 tabs │
 BOTTOM ───────▶│ - Sync views │      │ - Smooth     │     │ - Summary│
                └──────────────┘      │ - Scale      │     └──────────┘
                                      └──────┬───────┘
                                             │              ┌──────────┐
                                      ┌──────▼───────┐     │ PNG Plots│
                                      │PhaseDetector │────▶│ - 4 figs │
                                      │StepDetector  │     │ - 300dpi │
                                      │ - Walking    │     └──────────┘
                                      │ - Steps      │
                                      └──────┬───────┘     ┌──────────┐
                                             │             │ CSVs     │
                                      ┌──────▼───────┐    │ - CoM    │
                                      │MetricsCompute│───▶│ - Paws   │
                                      │ - Gait       │    └──────────┘
                                      │ - ROM        │
                                      └──────────────┘


3. CURRENT ARCHITECTURE ISSUES
===============================

🔴 CRITICAL ISSUES:
  ┌─────────────────────────────────────────────────────────┐
  │ 1. cli.py::run_pipeline() = GOD FUNCTION (232 lines)   │
  │    - 10 responsibilities in single function             │
  │    - Impossible to test individual stages               │
  │    - No checkpointing or recovery                       │
  └─────────────────────────────────────────────────────────┘

⚠️  MAJOR ISSUES:
  ┌─────────────────────────────────────────────────────────┐
  │ 2. Zero test coverage (tests/ directory empty)          │
  │ 3. No interface abstractions (tight coupling)           │
  │ 4. Version fragmentation (v1.1 vs v1.2 methods)         │
  │ 5. Hardcoded dependencies throughout                    │
  └─────────────────────────────────────────────────────────┘

⚠️  MEDIUM ISSUES:
  ┌─────────────────────────────────────────────────────────┐
  │ 6. Config validation missing (YAML with no schema)      │
  │ 7. Visualizer duplication (2 similar classes)           │
  │ 8. Magic numbers scattered (FPS = 120.0 everywhere)     │
  │ 9. No extensibility (hardcoded metrics pipeline)        │
  └─────────────────────────────────────────────────────────┘


4. PROPOSED v2.0 ARCHITECTURE
==============================

┌───────────────────────────────────────────────────────────────┐
│                   APPLICATION LAYER                            │
│  ┌──────────┐      ┌──────────┐      ┌──────────┐            │
│  │ CLI      │      │ REST API │      │ Plugins  │            │
│  └────┬─────┘      └────┬─────┘      └────┬─────┘            │
└───────┼──────────────────┼──────────────────┼────────────────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           ▼
┌───────────────────────────────────────────────────────────────┐
│                    DOMAIN LAYER                                │
│  ┌────────────────────────────────────────────────────┐       │
│  │         Pipeline Orchestrator                       │       │
│  │  - Stage Registry (Factory Pattern)                │       │
│  │  - Event Bus (Observer Pattern)                    │       │
│  │  - Checkpoint Manager                              │       │
│  │  - Error Recovery                                  │       │
│  └────────────────────────────────────────────────────┘       │
│                                                                │
│  Interfaces (Abstractions):                                   │
│    IDataLoader  IPreprocessor  IPhaseDetector                │
│    IStepDetector  IMetricsComputer  IExporter                │
└────────────────────────────────────────────────────────────────┘
                           ▼
┌────────────────────────────────────────────────────────────────┐
│              PIPELINE STAGES (Implementations)                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │DLC Loader    │  │Savgol Preproc│  │MAD Phase Det │        │
│  │SLEAP Loader  │  │Median Preproc│  │Adaptive Det  │        │
│  │OpenPose Ldl  │  └──────────────┘  └──────────────┘        │
│  └──────────────┘                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │Vertical Det  │  │Gait Metrics  │  │XLSX Export   │        │
│  │Velocity Det  │  │ROM Metrics   │  │CSV Export    │        │
│  │Hybrid Det    │  │Custom Metrics│  │JSON Export   │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
└────────────────────────────────────────────────────────────────┘
                           ▼
┌────────────────────────────────────────────────────────────────┐
│              INFRASTRUCTURE LAYER                              │
│  Config (Pydantic)  │  Persistence  │  Distributed (Ray)      │
│  Logging (structlog)│  Caching      │  Metrics (Prometheus)   │
└────────────────────────────────────────────────────────────────┘


5. KEY METRICS
==============

Codebase Statistics:
  Total lines:              4,926 LOC
  Largest file:             metrics_computer.py (694 LOC)
  God function:             run_pipeline() (232 LOC) 🔴
  
Module Distribution:
  core/                     498 LOC (10%)
  analysis/                 1,306 LOC (27%)
  export/                   1,728 LOC (35%)
  utils/                    800 LOC (16%)
  statistics/               276 LOC (6%)
  cli/orchestration         316 LOC (6%)
  
Quality Indicators:
  Test coverage:            0% 🔴
  Interface abstractions:   0 🔴
  Cyclomatic complexity:    High (God function) 🔴
  Coupling:                 Tight (10 imports in cli.py) ⚠️
  Cohesion:                 Good (module level) ✅
  
Scalability:
  Per-sample time:          ~6-7 seconds
  Memory per sample:        ~35 MB
  Batch parallelism:        ✅ (ProcessPoolExecutor)
  Distributed support:      ❌ (single machine only)
  Max dataset size:         1000 samples (~2 hours with 4 workers)


6. MIGRATION ROADMAP
====================

Phase 1: Foundation (2 weeks)
  ✓ Add Pydantic config schema
  ✓ Extract pipeline interfaces
  ✓ Write first 10 unit tests
  
Phase 2: Refactor Core (3 weeks)
  ✓ Break down God function
  ✓ Implement stage pattern
  ✓ Add event bus
  
Phase 3: Plugin System (2 weeks)
  ✓ Metric plugins
  ✓ Data loader plugins
  
Phase 4: Production Hardening (3 weeks)
  ✓ Structured logging
  ✓ Distributed processing (Ray)
  ✓ Performance optimization
  
Phase 5: API & Docs (2 weeks)
  ✓ REST API (FastAPI)
  ✓ Comprehensive documentation


TOTAL EFFORT: 12 weeks (1 architect + 1 developer)
RISK LEVEL:   Medium (incremental approach)
PRIORITY:     High (production readiness)

=========================================================
